services:
  loomdesk:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - .env.local
    environment:
      NODE_ENV: production
      DATA_DIR: /data
    volumes:
      - loomdesk-data:/data
    restart: unless-stopped
    init: true
volumes:
  loomdesk-data:
