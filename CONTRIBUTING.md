# 参与开发

使用 Node.js 24+，运行 `npm ci`、`npm run setup`、`npm run dev`。

提交前运行 `npm run typecheck`、`npm test`、`npm run build`。涉及 API 接口的变更应核对当前官方 OpenAPI，保持测试使用模拟服务，不把真实 Key、输入或产物加入仓库。

执行链路必须保留“校验 → 报价 → 用户确认 → 执行”，并在相同请求的模糊失败重试中复用原幂等键。新增接口必须检查会话隔离和请求来源，禁止提供任意 URL 代理或前端可改的密钥目的地址。

界面文案使用简体中文。演示功能要明确标注，不能用模拟成功替代真实服务错误。
