# Production: generate with node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_ENCRYPTION_KEY=
APP_ORIGIN=http://localhost:3000
COOKIE_SECURE=false
DATA_DIR=./.data
# Optional. Defaults to the official Shengsuanyun LoomLoom server. HTTPS required.
LOOMLOOM_BASE_URL=https://loomloom.shengsuanyun.com
